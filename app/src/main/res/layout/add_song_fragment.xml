<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/add_song_fragment"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:importantForAutofill="no"
    tools:context="ru.hokan.music_choser.ui.songs.add.AddSongFragment">

    <EditText
        android:id="@+id/add_song_artist_name"
        android:layout_width="@dimen/add_song_layout_text_width"
        android:layout_height="@dimen/add_song_layout_text_height"
        android:inputType="textPersonName"
        android:hint="@string/artist_name_text_field_hint"
        android:textSize="@dimen/add_song_layout_text_size"
        android:layout_gravity="center"
        android:layout_marginTop="240dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="1" />

    <EditText
        android:id="@+id/add_song_song_name"
        android:layout_width="@dimen/add_song_layout_text_width"
        android:layout_height="@dimen/add_song_layout_text_height"
        android:textSize="@dimen/add_song_layout_text_size"
        android:inputType="textPersonName"
        android:hint="@string/song_name_text_field_hint"
        android:layout_gravity="center"
        android:layout_marginTop="64dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toEndOf="@+id/add_song_artist_name"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="1" />

    <ImageButton
        android:id="@+id/submit_add_song_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginTop="120dp"
        android:layout_marginEnd="@dimen/add_song_button_padding"
        android:layout_marginBottom="@dimen/add_song_button_padding"
        android:src="@mipmap/add_song" />

</LinearLayout>